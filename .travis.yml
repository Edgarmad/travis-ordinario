language: java
jdk: oraclejdk11

services:
 - mysql
before_install:
 - sudo mysql -e 'CREATE USER 'calidaduser'@'localhost' IDENTIFIED BY 'FoXterr1er';'
 - sudo mysql -e 'GRANT ALL PRIVILEGES ON *.* TO 'calidaduser'@'localhost' WITH GRANT OPTION;'
 - sudo mysql -e 'CREATE DATABASE IF NOT EXISTS calidad2;'
 - sudo mysql -e 'select database();'
 - sudo mysql -e 'SHOW DATABASES;'
 - sudo mysql -e 'USE calidad2;'
 - sudo mysql -e 'select database();'
 - sudo mysql --version

after_success: echo "I´m done"
