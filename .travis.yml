language: java
jdk: oraclejdk11

services:
 - mysql
before_install:
 - sudo mysql -e 'select User from mysql.user;'
 - sudo mysql -e 'CREATE DATABASE IF NOT EXISTS calidad2;'
 - sudo mysql -e 'USE calidad2;'
 - sudo mysql -e 'select database();'
 - sudo mysql -e 'CREATE TABLE IF NOT EXISTS Alumno2(alumno_id INTEGER UNSIGNED PRIMARY KEY AUTO_INCREMENT,alumno_name VARCHAR(20) DEFAULT 'Jonh',alumno_LastNameP VARCHAR(20) DEFAULT 'Doe',age TINYINT(3)UNSIGNED DEFAULT 18,average float DEFAULT 0.0);'
 - sudo mysql -e 'SHOW DATABASES;'
 - sudo mysql --version

after_success: echo "I´m done"
